import { id, type TranslationKeys } from "./id";
import { en } from "./en";

export type { TranslationKeys };

export const translations: Record<string, TranslationKeys> = { id, en };

export type { TranslationKeys as TKeys };
